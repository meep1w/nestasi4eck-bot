# Pocket Option Bot (MVP skeleton)

- Python 3.11 recommended
- aiogram v3, SQLAlchemy (async), SQLite (aiosqlite)

## Quick start (dev)
1. Copy `.env.example` to `.env` and fill values.
2. `pip install -r requirements.txt`
3. `python -m app.main`
